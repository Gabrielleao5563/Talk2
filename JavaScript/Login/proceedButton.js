//Function that runs when the continue button is pressed
function proceedProcess(){

    switch(mode){

        //Login
        case 1:
            break;

        //Signup
        case 2:
            break;

        //Error
        default:

            console.error("The page is actually on a invalid position to continue");
            break;

    }

}